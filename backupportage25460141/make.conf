# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe"
CFLAGS="-Ofast -march=native -falign-functions=32 -mfpmath=both -pipe -funroll-loops -flto=8 -fgraphite-identity -floop-nest-optimize -malign-data=cacheline -mtls-dialect=gnu2 -Wl,--hash-style=gnu"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

CFLAGS="-Ofast -march=native -falign-functions=32 -mfpmath=both -pipe -funroll-loops -flto=8 -fgraphite-identity -floop-nest-optimize -malign-data=cacheline -mtls-dialect=gnu2 -Wl,--hash-style=gnu"
CXXFLAGS="${CFLAGS}"
CPU_FLAGS_X86="mmx mmxext sse sse2 ssse3 sse3"
ACCEPT_LICENSE="*"
MAKEOPTS="-j8"
PORTAGE_NICENESS=19
#PORTAGE_BINHOST="https://cloveros.ga"
#EMERGE_DEFAULT_OPTS="--jobs=4 --keep-going=y --autounmask-write=y -G"
#ACCEPT_KEYWORDS="**"
#binhost_mirrors="$PORTAGE_BINHOST,https://useast.cloveros.ga,https://uswest.cloveros.ga,https://ca.cloveros.ga,https://fr.cloveros.ga,https://nl.cloveros.ga,https://uk.cloveros.ga,https://au.cloveros.ga,https://sg.cloveros.ga,https://jp.cloveros.ga,"
#FETCHCOMMAND_HTTPS="sh -c \"aria2c -x2 -s99 -j99 -k1M -d \"\${DISTDIR}\" -o \"\${FILE}\" \\\$(sed -e \"s#,#\${DISTDIR}/\${FILE}\"\ \"#g\" -e \"s#$PKGDIR##g\" -e \"s#.partial##g\" <<< $binhost_mirrors) & aria2c --allow-overwrite -d \"\${DISTDIR}\" -o \"\${FILE}.asc\" \\\$(sed -e \"s#,#/s/signatures/\${DISTDIR}/\${FILE}.asc\"\ \"#g\" -e \"s#$PKGDIR##g\" -e \"s#.partial##g\" <<< $binhost_mirrors) && wait && gpg --verify \"\${DISTDIR}/\${FILE}.asc\" \"\${DISTDIR}/\${FILE}\" && rm \"\${DISTDIR}/\${FILE}.asc\"\""
USE="-systemd -pulseaudio -avahi -dbus -consolekit -libnotify -udisks \
     -zeroconf -nls -doc -gnome-keyring -gstreamer -libav -openal \
     -kde -gnome -openssl libressl bindist ipv6 cli jpeg gif png exif \
     lcms raw openexr joystick alsa xinerama wayland otr minimal \
     offensive zsh-completion custom-cflags custom-optimization \
     threads aio jit fftw lto graphite pgo numa egl dga dri dri3 \
     vulkan opengl opencl glamor vaapi vdpau system-ffmpeg system-icu \
     system-libvpx system-harfbuzz system-jpeg system-libevent \
     system-sqlite system-cairo system-compress system-images \
     system-nss system-pixman system-vpx system-llvm system-lua \
     system-cmark system-libyaml system-lcms system-lz4 system-uulib \
     system-snappy system-binutils system-clang system-tbb system-renpy \
     system-libs system-heimdal system-leveldb system-libmspack \
     system-zlib system-av1 system-digest networkmanager"
